name: CI

# Validates every PR (and every push to main): applies the patches and builds all
# package formats for amd64, split across parallel jobs (mirroring release.yml's
# fetch -> build-format -> build-arch-pkg pipeline) instead of one job building
# all five formats sequentially. Only the tarball is uploaded between jobs (so
# build-arch-pkg can reuse it, same as build-arch.sh does locally); no format's
# output is kept afterwards - proving the build succeeds is the point, not
# producing artifacts (the real ones are published by release.yml).
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # Cheap gate: skip the expensive build jobs below when a push/PR only touches
  # docs/config files that can't affect the build. Defaults to "build" whenever
  # the diff base can't be resolved (e.g. a brand-new branch) or anything about
  # the check itself goes wrong - always fail toward validating, never toward
  # silently skipping it. The CI job below still runs unconditionally (if:
  # always()), so a skip here never leaves the required check stuck pending.
  changes:
    runs-on: ubuntu-latest
    outputs:
      build: ${{ steps.check.outputs.build }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - id: check
        env:
          BASE: ${{ github.event.pull_request.base.sha || github.event.before }}
        run: |
          build=true
          if [ -n "$BASE" ] && git cat-file -e "$BASE" 2>/dev/null; then
            if ! git diff --name-only "$BASE" HEAD | grep -qvE '^(.*\.md|LICENSE|\.gitignore|\.gitleaks\.toml|\.gitleaks-version)$'; then
              build=false
            fi
          fi
          echo "build=$build" >> "$GITHUB_OUTPUT"

  fetch:
    needs: changes
    if: needs.changes.outputs.build == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Detect upstream version (for the cache key only)
        # fetch-deb.sh re-detects it below regardless, so a race with a
        # version bump between these two calls just costs a cache miss, never
        # a stale build.
        id: v
        run: |
          eval "$(bash scripts/detect-version.sh amd64)"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
      - name: Restore cached payload
        # Keyed on the upstream version plus a hash of everything that can
        # change what fetch-deb.sh produces for it (patches, the scripts that
        # apply/validate them, the native-path allowlist, our pinned deps).
        # Most pushes only touch packaging/CI files, not patches or upstream
        # version, so this skips the ~200MB download/verify/patch on most CI
        # runs instead of repeating it on every push.
        id: cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: payload.tar
          key: payload-amd64-${{ steps.v.outputs.version }}-${{ hashFiles('patches/**', 'scripts/patch-payload.mjs', 'scripts/check-native-paths.mjs', 'scripts/fetch-deb.sh', 'scripts/detect-version.sh', 'packaging/launcher/claude-desktop-hotkey', 'baseline/system-paths.json', 'package-lock.json') }}
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        if: steps.cache.outputs.cache-hit != 'true'
        with:
          node-version: "24"
          cache: npm
      - run: npm ci
        if: steps.cache.outputs.cache-hit != 'true'
      - name: Install fetch tooling
        if: steps.cache.outputs.cache-hit != 'true'
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends binutils xz-utils curl
      - name: Fetch + verify + patch the official .deb
        if: steps.cache.outputs.cache-hit != 'true'
        run: bash scripts/fetch-deb.sh amd64 payload
      - name: Archive payload
        # actions/upload-artifact strips executable bits and dereferences
        # symlinks (e.g. bundled .so sonames become non-executable duplicate
        # regular-file copies), which broke every packaged format in
        # release.yml. Tar it into one file so the archive format itself
        # carries that metadata through untouched.
        if: steps.cache.outputs.cache-hit != 'true'
        run: tar -cf payload.tar payload
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: payload-amd64
          path: payload.tar
          retention-days: 1   # only needed by build-format moments later

  # rpm/tarball/appimage/deb, one parallel job per format (mirrors release.yml's
  # build-format matrix), all validating against the payload fetch fetched above.
  build-format:
    needs: [changes, fetch]
    if: needs.changes.outputs.build == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - format: rpm
            packages: rpm
          - format: deb
            packages: dpkg binutils xz-utils
          - format: tarball
            packages: ""
          - format: appimage
            packages: curl zsync
    env:
      APPIMAGE_EXTRACT_AND_RUN: '1'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install build tooling
        if: matrix.packages != ''
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends ${{ matrix.packages }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: payload-amd64
          path: .
      - name: Unpack payload
        # Restores the exact permissions/symlinks fetch-deb.sh produced (see
        # the "Archive payload" step in the fetch job above).
        run: |
          tar -xf payload.tar
          test -x payload/payload/usr/lib/claude-desktop/claude-desktop \
            || { echo "unpack-payload: main binary lost its executable bit in transit" >&2; exit 1; }
          test -x payload/payload/usr/bin/claude-desktop \
            || { echo "unpack-payload: launcher lost its executable bit in transit" >&2; exit 1; }
      - name: Build ${{ matrix.format }} (amd64) to validate patches + build script
        env:
          PAYLOAD_DIR: ${{ github.workspace }}/payload
          # CI never uploads or publishes anything (see header), so the AppImage's
          # update-info + .zsync delta file (a rolling checksum over the whole
          # ~150-200MB AppImage) would be pure wasted work here. build-appimage.sh
          # only builds that when $GITHUB_REPOSITORY is set, which the Actions
          # runner does automatically - override it empty, scoped to just this
          # step (unlike checkout/download-artifact above, which do rely on it).
          GITHUB_REPOSITORY: ''
        run: bash "scripts/build-${{ matrix.format }}.sh" amd64 dist
      # Only the tarball is needed by build-arch-pkg below; the other formats'
      # output just has to build successfully, not be kept anywhere.
      - name: Upload tarball (only format build-arch-pkg needs)
        if: matrix.format == 'tarball'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: dist-amd64-tarball
          path: dist/*
          retention-days: 1

  # Arch .pkg.tar.zst via makepkg in an archlinux container, reusing the tarball
  # build-format already produced (same as build-arch.sh's local reuse path).
  build-arch-pkg:
    needs: [changes, build-format]
    if: needs.changes.outputs.build == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: dist-amd64-tarball
          path: dist
      - name: Compute weekly cache bucket
        id: cachekey
        run: echo "week=$(date -u +%G-W%V)" >> "$GITHUB_OUTPUT"
      - name: Cache pacman packages (archlinux base-devel, for build-arch.sh)
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: pacman-cache
          # Same key as release.yml's build-arch-pkg (no arch suffix - it's the
          # same x86_64 packages either way): whichever workflow runs first each
          # week populates the cache for the other too, since caches are shared
          # repo-wide, not scoped per workflow file.
          key: pacman-${{ steps.cachekey.outputs.week }}
      - name: Cache archlinux base image
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: archlinux-image-cache
          # Same key as release.yml's build-arch-pkg, for the same reason.
          key: archlinux-image-${{ steps.cachekey.outputs.week }}
      - name: Load or pull the archlinux base image
        # Only the pacman *packages* installed inside the container were cached
        # before; the base image layers themselves were still pulled from Docker
        # Hub on every run. Load it from the cache above when present, else pull
        # once and save it so this week's later runs load instead of pulling.
        run: |
          mkdir -p archlinux-image-cache
          if [ -f archlinux-image-cache/archlinux.tar ]; then
            docker load -i archlinux-image-cache/archlinux.tar
            echo "build-arch: loaded archlinux:latest from cache"
          else
            docker pull archlinux:latest
            docker save archlinux:latest -o archlinux-image-cache/archlinux.tar
            echo "build-arch: pulled archlinux:latest and cached it"
          fi
      - name: Build Arch package (validates build-arch.sh, no upload)
        env:
          PACMAN_CACHE_DIR: ${{ github.workspace }}/pacman-cache
        run: bash scripts/build-arch.sh amd64 dist

  # Required-status-check gate: branch protection expects a check named "CI".
  # Splitting the format builds above means none of those jobs is individually
  # named "CI" anymore, so this job exists purely to be that gate. `if: always()`
  # is required here: fetch/build-format/build-arch-pkg are skipped (not failed)
  # on a docs/config-only change, and without always() this job would inherit
  # that skip too, leaving the required check stuck "Pending" forever instead of
  # resolving green.
  CI:
    name: CI
    needs: [changes, fetch, build-format, build-arch-pkg]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Check build jobs
        run: |
          for r in "${{ needs.fetch.result }}" "${{ needs.build-format.result }}" "${{ needs.build-arch-pkg.result }}"; do
            if [ "$r" != "success" ] && [ "$r" != "skipped" ]; then
              echo "::error::a build job did not succeed: $r"
              exit 1
            fi
          done
          echo "All formats built OK across parallel jobs (not uploaded), or skipped: docs/config-only change."
